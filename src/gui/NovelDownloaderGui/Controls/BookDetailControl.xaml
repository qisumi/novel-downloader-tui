<UserControl
    x:Class="NovelDownloaderGui.Controls.BookDetailControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Border
        Padding="16"
        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
        BorderThickness="1"
        CornerRadius="16">
        <Grid RowSpacing="14">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Spacing="6">
                <TextBlock
                    x:Name="EmptyStateText"
                    FontSize="14"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="从左侧选择一本书，右侧会展示目录、缓存和导出操作。"
                    Visibility="Visible" />
                <TextBlock
                    x:Name="BookTitleText"
                    FontSize="28"
                    FontWeight="SemiBold"
                    Text="未选择书籍"
                    TextWrapping="Wrap" />
                <TextBlock
                    x:Name="BookMetaText"
                    FontSize="14"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="作者 / 分类 / 字数 / 评分" />
            </StackPanel>

            <TextBlock
                x:Name="BookAbstractText"
                Grid.Row="1"
                MaxLines="5"
                TextWrapping="WrapWholeWords" />

            <StackPanel
                Grid.Row="2"
                Orientation="Horizontal"
                Spacing="8">
                <Button
                    x:Name="SaveButton"
                    Click="OnSaveClicked"
                    Content="加入书架" />
                <Button
                    x:Name="RemoveButton"
                    Click="OnRemoveClicked"
                    Content="移出书架"
                    Visibility="Collapsed" />
                <Button
                    Click="OnLoadTocClicked"
                    Content="加载目录" />
                <Button
                    Click="OnDownloadClicked"
                    Content="缓存全书" />
                <Button
                    Click="OnExportEpubClicked"
                    Content="导出 EPUB" />
                <Button
                    Click="OnExportTxtClicked"
                    Content="导出 TXT" />
            </StackPanel>

            <StackPanel
                Grid.Row="3"
                Orientation="Horizontal"
                Spacing="12">
                <CheckBox
                    x:Name="ForceRemoteCheckBox"
                    Content="强制远程刷新" />
                <TextBlock
                    VerticalAlignment="Center"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="导出默认覆盖当前已加载目录的全部章节。" />
            </StackPanel>

            <Grid Grid.Row="4" RowSpacing="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" Spacing="12">
                    <TextBlock
                        x:Name="TocSummaryText"
                        FontSize="14"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Text="目录未加载" />
                    <ProgressRing
                        x:Name="BusyRing"
                        Height="18"
                        IsActive="False"
                        Width="18" />
                </StackPanel>

                <InfoBar
                    x:Name="StatusBar"
                    Grid.Row="1"
                    IsOpen="False"
                    Severity="Informational" />

                <ListView
                    x:Name="TocListView"
                    Grid.Row="2"
                    SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Spacing="2">
                                    <TextBlock Text="{Binding Title}" TextWrapping="WrapWholeWords" />
                                    <TextBlock
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Text="{Binding VolumeName}" />
                                </StackPanel>
                                <TextBlock
                                    Grid.Column="1"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="{Binding WordCount}" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Grid>
    </Border>
</UserControl>
