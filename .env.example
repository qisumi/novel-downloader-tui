# 番茄小说 TUI 下载器 - .env 示例
# 用法：
#   1. 复制本文件为 .env
#   2. 按需修改下面的值
#   3. 程序启动时会自动读取项目根目录下的 .env
#
# 配置优先级：
#   命令行参数 > 系统环境变量 > 本文件

# API 密钥（对应 -k / --apikey）
# 对于fanqie.lua插件必填。请替换为你自己的有效 Key。
FANQIE_APIKEY=your_api_key_here

# SQLite 数据库路径（对应 --db），默认 fanqie.db
FANQIE_DB=fanqie.db

# EPUB/TXT 输出目录（对应 -o / --epub-dir），默认当前目录
FANQIE_EPUB_DIR=.
